# ASP.NET
# Build and test ASP.NET projects.
# Add steps that publish symbols, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/apps/aspnet/build-aspnet-4

trigger:
- StableBuild

pool:
  vmImage: 'windows-latest'

variables:
  solution: '**/*.sln'
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'

steps:
- task: NuGetToolInstaller@1
  inputs:
    versionSpec: '5.0.2'
    checkLatest: true

- task: PkgESInstallNuGetToolsPackage@10
  inputs:
        packageName: 'Brutal.Dev.StrongNameSigner'
        packageVersion: '2.3.0'
        addToFrontPath: false
        installRoot: '$(Build.SourcesDirectory)/Packages'

- task: BatchScript@1
  inputs:
    filename: 'build.bat'
    arguments: 'pack'

- task: NuGetCommand@2
  inputs:
    command: 'push'
    feedsToUse: 'select'
    packagesToPush: '$(Build.SourcesDirectory)/Build/*.nupkg'
    nuGetFeedType: 'internal'
    publishVstsFeed: 'internal'
    versioningScheme: 'off'